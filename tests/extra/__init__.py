"""Additional edge-case and happy-path coverage (tier: tests/extra).

These intentionally do NOT duplicate the strict suite in ``tests/`` or the
parity tier in ``exploration/``, they target gaps: result-type ergonomics
beyond the regression fit, psi/invtr2 identities, cross-estimator consistency,
formula-shape variety, and compat/help completeness.
"""
