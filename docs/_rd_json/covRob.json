{
  "name": "covRob",
  "aliases": [
    "covRob",
    "Multirobu"
  ],
  "title": "Robust multivariate location and scatter estimators",
  "description": "This function computes robust estimators for multivariate location and scatter.",
  "usage": "covRob(X, type = \"auto\", maxit = 50, tol = 1e-04, corr = FALSE)",
  "arguments": [
    {
      "r_name": "X",
      "description": "a data matrix with observations in rows."
    },
    {
      "r_name": "type",
      "description": "a string indicating which estimator to compute. Valid options\nare \"Rocke\" for Rocke's S-estimator, \"MM\" for an MM-estimator with a\nSHR rho function, or \"auto\" (default) which selects \"Rocke\" if the number\nof variables is greater than or equal to 10, and \"MM\" otherwise."
    },
    {
      "r_name": "maxit",
      "description": "Maximum number of iterations, defaults to 50."
    },
    {
      "r_name": "tol",
      "description": "Tolerance for convergence, defaults to 1e-4."
    },
    {
      "r_name": "corr",
      "description": "A logical value. If `TRUE` a correlation matrix is included in the element `cor` of the returned object. Defaults to `FALSE`."
    }
  ],
  "value": [
    {
      "r_name": "center",
      "description": "The location estimate."
    },
    {
      "r_name": "cov",
      "description": "The scatter matrix estimate, scaled for consistency at the normal distribution."
    },
    {
      "r_name": "cor",
      "description": "The correlation matrix estimate, if the argument `cor` equals `TRUE`. Otherwise it is set to `NULL`."
    },
    {
      "r_name": "dist",
      "description": "Robust Mahalanobis distances"
    },
    {
      "r_name": "wts",
      "description": "weights"
    },
    {
      "r_name": "call",
      "description": "an image of the call that produced the object with all the arguments named. The matched call."
    },
    {
      "r_name": "mu",
      "description": "The location estimate. Same as `center` above."
    },
    {
      "r_name": "V",
      "description": "The scatter matrix estimate, scaled for consistency at the normal distribution. Same as `cov` above."
    }
  ],
  "details": "This function computes robust estimators for multivariate location and scatter.\nThe default behaviour (`type = \"auto\"`) computes a \"Rocke\" estimator\n(as implemented in ``covRobRocke``) if the number\nof variables is greater than or equal to 10, and an MM-estimator with a\nSHR rho function (as implemented in ``covRobMM``) otherwise.",
  "sections": {},
  "examples_r": "data(bus)\nX0 <- as.matrix(bus)\nX1 <- X0[,-9]\ntmp <- covRob(X1)\nround(tmp$cov[1:10, 1:10], 3)\ntmp$mu",
  "seealso": [
    "covRobRocke",
    "covRobMM"
  ],
  "references": "<http://www.wiley.com/go/maronna/robust>",
  "keywords": [],
  "author": "Ricardo Maronna, <rmaronna@retina.ar>"
}